# إعداد قاعدة بيانات Railway باسم "muq" والحفاظ على البيانات

## الهدف
ربط المشروع بقاعدة بيانات PostgreSQL باسم "muq" على Railway والحفاظ على جميع البيانات عند تحديث Git.

## الخطوات

### 1. إضافة خدمة PostgreSQL في Railway

1. اذهب إلى مشروعك في [Railway](https://railway.app/)
2. انقر على **New Project** أو اختر مشروعك الحالي
3. انقر على **+ New Service**
4. اختر **Database**
5. اختر **PostgreSQL**
6. Railway سيقوم بإنشاء قاعدة بيانات PostgreSQL تلقائياً

### 2. تسمية قاعدة البيانات

Railway يسمي قاعدة البيانات تلقائياً. لضمان أن اسمها "muq":

1. بعد إنشاء خدمة PostgreSQL، انقر عليها
2. انتقل إلى **Settings** tab
3. ابحث عن اسم قاعدة البيانات (Database Name)
4. إذا لم يكن "muq"، يمكنك طلب تغييره من Railway أو استخدام الاسم الموجود

**ملاحظة:** Railway لا يسمح دائماً بتغيير اسم قاعدة البيانات بعد الإنشاء، لكن يمكنك استخدام الاسم الموجود والربط به في الكود.

### 3. ربط قاعدة البيانات بالمشروع

Railway يربط قاعدة البيانات تلقائياً بالمتغير `DATABASE_URL`. للتأكد من الربط الصحيح:

1. في Railway، انقر على مشروعك
2. انتقل إلى **Variables** tab
3. تأكد من وجود متغير `DATABASE_URL` تلقائياً من Railway
4. إذا لم يكن موجوداً، أضفه يدوياً:
   ```
   DATABASE_URL=postgresql://username:password@host:port/database_name
   ```

### 4. الحفاظ على البيانات عند تحديث Git

المشروع مُعد بالفعل للحفاظ على البيانات:

#### محميات في .gitignore:
```gitignore
*.sqlite3
*.sqlite3-journal
muq.sqlite3
muq.sqlite3-journal
/media/
```

هذا يمنع Git من تتبع ملفات قاعدة البيانات المحلية.

#### إعدادات Django في settings.py:
```python
DATABASES = {
    'default': dj_database_url.config(
        default=database_url,
        conn_max_age=600,
        conn_health_checks=True,
    )
}
```

هذا يستخدم قاعدة البيانات على Railway (PostgreSQL) بدلاً من SQLite المحلي.

### 5. التعامل مع الترحيلات (Migrations)

للحفاظ على البيانات عند تحديث الكود:

1. **لا تحذف ملفات الترحيلات**:
   - `properties/migrations/` محمية في .gitignore بشكل افتراضي
   - لا تحذف ملفات الترحيلات الموجودة

2. **استخدم migrate آمن**:
   ```bash
   python manage.py migrate
   ```
   Django سيتعامل مع التغييرات بشكل آمن ولن يحذف البيانات

3. **فحص الترحيلات قبل النشر**:
   ```bash
   python manage.py makemigrations --dry-run
   python manage.py migrate --plan
   ```

### 6. استخدام قاعدة بيانات محلية للتطوير

للتطوير المحلي مع الحفاظ على البيانات:

1. إنشاء قاعدة بيانات محلية:
   ```bash
   python manage.py migrate
   ```

2. البيانات المحلية ستبقى في `db.sqlite3` (محفية من Git)

3. عند النشر إلى Railway، سيتم استخدام قاعدة البيانات السحابية

### 7. النسخ الاحتياطي للبيانات

للحفاظ الإضافي على البيانات:

#### من Railway:
1. اذهب إلى خدمة PostgreSQL في Railway
2. انقر على **Export** لتنزيل نسخة احتياطية
3. احفظ النسخة الاحتياطية محلياً

#### باستخدام Django:
```bash
python manage.py dumpdata > backup.json
```

### 8. التحقق من الاتصال بقاعدة البيانات

للتأكد من أن المشروع متصل بقاعدة بيانات Railway:

1. في Railway، شاهد سجلات (Logs) المشروع
2. ابحث عن خطوط مثل:
   ```
   DATABASES = {...}
   ```
3. تأكد من استخدام PostgreSQL وليس SQLite

### 9. تحديث Git بأمان

عند تحديث الكود من Git:

1. **اسحب التغييرات**:
   ```bash
   git pull origin main
   ```

2. **طبق الترحيلات الجديدة فقط**:
   ```bash
   python manage.py migrate
   ```

3. **لا تستخدم flush أو إعادة تعيين قاعدة البيانات**:
   ```bash
   # لا تستخدم هذه الأوامر:
   python manage.py flush
   python manage.py reset_db
   ```

### 10. مراقبة حالة قاعدة البيانات

في Railway:
- راقب استخدام مساحة التخزين
- راقب أداء قاعدة البيانات
- قم بتحديث Railway إلى أحدث إصدار إذا لزم الأمر

## استكشاف الأخطاء

### قاعدة البيانات فارغة بعد التحديث
- تأكد من استخدام PostgreSQL وليس SQLite
- تحقق من متغير `DATABASE_URL` في Railway
- راجع سجلات Railway

### أخطاء الترحيل
- استخدم `python manage.py showmigrations` لفحص الترحيلات
- استخدم `python manage.py migrate --fake` إذا لزم الأمر
- لا تحذف ملفات الترحيلات

### مشاكل الاتصال
- تحقق من إعدادات Railway
- تأكد من أن خدمة PostgreSQL قيد التشغيل
- راجع سجلات الأخطاء

## ملخص

✅ **ما تم إعداده:**
- قاعدة بيانات PostgreSQL على Railway
- حماية ملفات قاعدة البيانات في .gitignore
- إعدادات Django للاتصال بقاعدة البيانات السحابية
- إرشادات للحفاظ على البيانات

✅ **البيانات محفوظة لأن:**
- Railway يخزن البيانات بشكل منفصل عن الكود
- Git لا يتتبع ملفات قاعدة البيانات
- الترحيلات تعمل بشكل آمن
- النسخ الاحتياطية متاحة

✅ **عند تحديث Git:**
- اسحب التغييرات فقط
- طبق الترحيلات الجديدة
- البيانات تبقى في Railway
- لا تأثير على البيانات الموجودة
