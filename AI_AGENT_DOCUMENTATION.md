# AI Agent System Documentation - نظام الوكيل الذكي

## نظرة عامة

تم تحويل "مساعد موقعنا الذكي" إلى **AI Agent حقيقي** قادر على:
- فهم الهدف من المحادثة
- التخطيط للخطوات المناسبة
- استخدام أدوات الموقع بشكل ذكي
- تنفيذ الإجراءات مع التحقق
- الرد بشكل طبيعي وموجه

---

## المعمارية الكاملة

### دورة عمل AI Agent (Agent Loop)

```text
User Message
      ↓
Understand (فهم الرسالة)
      ↓
Analyze Context (تحليل السياق)
      ↓
Determine Goal (تحديد الهدف)
      ↓
Plan (التخطيط)
      ↓
Choose Tool (اختيار الأداة)
      ↓
Execute Tool (تنفيذ الأداة)
      ↓
Check Result (التحقق من النتيجة)
      ↓
Decide Next Action (تحديد الإجراء التالي)
      ↓
Respond (الرد)
```

---

## المكونات الجديدة

### 1. Tool Calling System
**الملف:** `ai_agent_tools.py`

#### الأدوات المتاحة:

#### Read Tools (أدوات القراءة)
- **search_properties** - البحث عن العقارات
- **get_property** - الحصول على تفاصيل عقار معين
- **search_agents** - البحث عن الدلالين
- **search_jobs** - البحث عن الوظائف

#### Write Tools (أدوات الكتابة)
- **save_property** - حفظ العقار في المفضلة
- **contact_agent** - التواصل مع دلال
- **create_property_listing** - إنشاء إعلان عقار جديد

#### نظام الصلاحيات:
- **READ** - يمكن استخدامها للبحث فقط
- **WRITE** - تحتاج تسجيل دخول
- **ADMIN** - للمسؤولين فقط

#### نظام التحقق:
- كل أداة لها schema للمدخلات
- التحقق من صحة البيانات قبل التنفيذ
- إرجاع رسائل خطأ واضحة

### 2. AI Agent Loop
**الملف:** `ai_agent_loop.py`

#### حالات Agent:
- **UNDERSTANDING** - فهم الرسالة
- **PLANNING** - التخطيط للتنفيذ
- **EXECUTING** - تنفيذ الأدوات
- **VERIFYING** - التحقق من النتائج
- **RESPONDING** - توليد الرد
- **WAITING_CONFIRMATION** - انتظار تأكيد المستخدم
- **ERROR** - حالة الخطأ

#### القدرات الرئيسية:
- فهم طلبات معقدة
- تخطيط خطوات التنفيذ
- استدعاء الأدوات بشكل صحيح
- التحقق من النتائج
- توليد ردود طبيعية

### 3. Confirmation System
**الملف:** `ai_agent_loop.py`

#### عمليات تتطلب تأكيد:
- حفظ العقار في المفضلة
- إرسال رسالة للدلال
- نشر إعلان عقار جديد
- أي عملية تغير البيانات

#### واجهة التأكيد:
- عرض ملخص العملية
- أزرار "نعم" و "لا"
- إلغاء عند الرفض

### 4. Agent Memory System
**الملف:** `ai_agent_loop.py`

#### طبقات الذاكرة:

**Short-Term Memory:**
- معلومات المحادثة الحالية
- النية الحالية
- المعلومات المستخرجة

**Session Memory:**
- حالة الجلسة عبر الصفحات
- تاريخ النوايا
- آخر نشاط

**Long-Term Memory:**
- تفضيلات البحث
- المحافظة المفضلة
- (قابل للتوسع مستقبلاً)

### 5. Dynamic Search Refinement
**الملف:** `ai_agent_loop.py`

#### التحسين الذكي للبحث:
- إذا كانت النتائج كثيرة (>50):
  - اقتراح تضييق البحث
  - أزرار "رفع الميزانية"، "توسيع المنطقة"
- تحديث البحث بدلاً من البدء من الصفر
- حفظ الفلات السابقة

### 6. Error Handling
**الملف:** `ai_agent_loop.py`

#### معالجة الأخطاء:
- رسائل خطأ ودية للمستخدم
- تسجيل الأخطاء التفصيلية في السجلات
- عدم كشف معلومات حساسة
- محاولات إعادة محدودة (3 محاولات)

---

## الـ API Endpoints الجديدة

### المحادثة الرئيسية
```http
POST /api/chatbot/
Content-Type: application/json

{
  "message": "أريد بيت بالبصرة",
  "conversation_id": "unique_id"
}
```

### التأكيد على العمليات
```http
POST /api/chatbot/confirmation/
Content-Type: application/json

{
  "conversation_id": "unique_id",
  "confirmed": true
}
```

### الملاحظات
```http
POST /api/chatbot/feedback/
Content-Type: application/json

{
  "conversation_id": "unique_id",
  "feedback": "positive",
  "message": "آخر رسالة"
}
```

### التصحيحات
```http
POST /api/chatbot/correction/
Content-Type: application/json

{
  "conversation_id": "unique_id",
  "original_intent": "buy_property",
  "corrected_intent": "sell_property",
  "message": "أريد أبيع"
}
```

### الإحصائيات
```http
GET /api/chatbot/statistics/
```

---

## أمثلة الاستخدام

### 1. بحث مع معلومات كاملة
```
المستخدم: أريد بيت بالبصرة بحدود 150 مليون
Agent: [فهم] [تخطيط] [تنفيذ أداة search_properties]
Agent: حلو، لكيت 7 عقارات مطابقة لطلبك 👍
```

### 2. طلب يتطلب تأكيد
```
المستخدم: أريد أحفظ هذا العقار
Agent: [فهم] [تخطيط] [تحديد أداة save_property]
Agent: تريد حفظ العقار "بيت في البصرة" في المفضلة؟
المستخدم: [يضغط نعم]
Agent: [تنفيذ] [تأكيد] تم الحفظ بنجاح 👍
```

### 3. تعديل الطلب
```
المستخدم: لا خلّي الميزانية 200 مليون
Agent: [فهم] [تحديد تعديل] [تحديث الفلات]
Agent: تمام، رحت أبحث بكرة... [نتائج محدثة]
```

### 4. طلب مركب
```
المستخدم: أريد بيت بالعراق ويفضل البصرة، عندي تقريباً 180 مليون وأريد منطقة هادئة وقريبة من المدارس
Agent: [فهم] [استخراج المعلومات] [تخطيط] [تنفيذ]
Agent: حلو، لكيت نتائج تناسب طلبك مع منطقة هادئة وقريبة من المدارس 👍
```

---

## Security & Safety

### Agent Guardrails
- ✅ عدم اختراع البيانات
- ✅ التحقق من الصلاحيات قبل كل عملية
- ✅ تأكيد العمليات المهمة
- ✅ عدم الوصول لبيانات مستخدم آخر
- ✅ عدم كشف معلومات خاصة
- ✅ منع تجاوز System Rules

### Protection Against Prompt Injection
- ✅ رفض تجاوز القواعد
- ✅ التحقق من صلاحيات الأدوات
- ✅ عدم تنفيذ أوامر غير مصرح بها
- ✅ التحقق من صحة المدخلات

---

## Performance & Observability

### التحسينات:
- ✅ Caching عند الحاجة
- ✅ Pagination للنتائج
- ✅ Timeouts للـ API
- ✅ محاولات إعادة محدودة
- ✅ Database Indexes

### المراقبة:
- ✅ تسجيل استخدام الأدوات
- ✅ تتبع معدلات النجاح
- ✅ قياس أوقات التنفيذ
- ✅ إحصائيات شاملة

---

## قابلية التوسع

### المعمارية تسمح بإضافة:
- ✅ نماذج LLM عربية
- ✅ نماذج Fine-Tuned
- ✅ نماذج Embedding
- ✅ قواعد بيانات Vector
- ✅ RAG (Retrieval-Augmented Generation)
- ✅ مصنفات Intent متقدمة
- ✅ نماذج NER
- ✅ أنظمة توصيات Learning-to-Rank

---

## الحالة الحالية

- ✅ **الخادم يعمل:** http://127.0.0.1:8000
- ✅ **المعاينة مفتوحة:** http://127.0.0.1:50673
- ✅ **الأدوات مسجلة:** 7 أدوات
- ✅ **Agent Loop مفعل:** يعمل بنجاح
- ✅ **نظام التأكيد:** جاهز
- ✅ **معالجة الأخطاء:** مفعلة

---

## الفرق بين المراحل

### المرحلة 1 (الأصلية):
- Chatbot يعتمد على أسئلة وأجوبة ثابتة
- بدون فهم حقيقي للغة
- بدون سياق محادثة

### المرحلة 2 (الذكاء):
- فهم اللغة العربية واللهجة العراقية
- معالجة سياق المحادثة
- استخراج المعلومات الذكي
- البحث الدلالي والهجين

### المرحلة 3 (Agent الحالي):
- **AI Agent حقيقي** بـ Agent Loop كامل
- Tool Calling مع صلاحيات
- Confirmation System
- تخطيط ذكي للخطوات
- تنفيذ وتحقق من النتائج
- معالجة أخطاء متقدمة
- نظام أمان متكامل

---

## الهدف النهائي

المساعد الآن يعمل كـ **موظف ذكي داخل المنصة**:

```text
يفهم ← يتذكر ← يحلل ← يبحث ← يقارن
↓
يسأل عند الحاجة ← يقترح ← ينفذ ← يتحقق ← يجيب
```

وليس مجرد:
```text
Button ← Question ← Answer
```

---

## المستقبل

### يمكن إضافة:
- نماذج Deep Learning حقيقية
- RAG للمعلومات الخاصة بالموقع
- نظام توصيات متقدم
- لوحة تحكم AI للمسؤولين
- خط أنابيب تدريب ML كامل
- Embeddings للبحث الدلالي
- قاعدة بيانات Vector

### بدون تغيير:
- واجهة Chatbot
- تعريف الأدوات
- Agent Loop الأساسي
- نظام التأكيد

---

## الخلاصة

تم بنجاح تحويل المساعد إلى **AI Agent حقيقي** قادر على:

- ✅ فهم المستخدم بشكل طبيعي
- ✅ تخطيط وتنفيذ العمليات
- ✅ استخدام أدوات الموقع بأمان
- ✅ التحقق من النتائج قبل الرد
- ✅ معالجة الأخطاء بذكاء
- ✅ الحفاظ على سياق المحادثة
- ✅ التعلم من التفاعلات
- ✅ قابل للتوسع مستقبلاً

النظام جاهز للاستخدام الإنتاجي مع إمكانيات تطوير مستقبلية ضخمة! 🚀