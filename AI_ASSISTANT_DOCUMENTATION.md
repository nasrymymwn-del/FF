# AI Assistant Documentation - مساعد موقعنا الذكي

## نظرة عامة

تم تطوير "مساعد موقعنا الذكي" من نظام Chatbot يعتمد على القواعد إلى نظام AI حقيقي قادر على فهم اللغة العربية واللهجة العراقية، مع معالجة سياق المحادثة والاستجابة بشكل طبيعي.

## المكونات الرئيسية

### 1. NLP Layer (الطبقة اللغوية)
**الملف:** `ai_nlp_layer.py`

- واجهة مجردة لنماذج NLP المختلفة
- دعم تبديل النماذج بدون تغيير الكود
- تنفيذ قائم على القواعد كنموذج أولي
- قابل للتوسع لدعم نماذج Deep Learning مستقبلًا

### 2. Intent Detection (تحديد النية)
**الملف:** `ai_intent_detection.py`

- نظام تحديد النية مع نظام ثقة (Confidence)
- دعم 12 نية أساسية:
  - شراء عقار، بيع عقار، الانضمام كدلال، البحث عن وظيفة
  - السفر، الفنادق، الخدمات، البناء، المزادات، المنتجات، المنتجعات
- نظام تصحيح للمستخدمين
- قابلة للتعديل ديناميكيًا
- إحصائيات دقيقة لأداء النظام

### 3. Entity Extraction (استخراج المعلومات)
**الملف:** `ai_entity_extraction.py`

- استخراج المعلومات من النص العربي
- دعم أنواع مختلفة من المعلومات:
  - نوع العقار، المحافظة، المنطقة، المساحة، الميزانية
  - نوع الوظيفة، الموقع، الراتب
  - نوع الغرفة، المدة
- تطبيع المعلومات
- نظام تصحيح دقيق
- التحقق من صحة المعلومات المستخرجة

### 4. Arabic Normalizer (المطبع العربي)
**الملف:** `ai_arabic_normalizer.py`

- تطبيع النص العربي واللهجة العراقية
- دعم أكثر من 50 كلمة لهجة عراقية
- تصحيح الأخطاء الإملائية
- فهم الأرقام المعقدة:
  - "نصف مليون" = 500,000
  - "مليون ونص" = 1,500,000
  - "ربع مليون" = 250,000
- تطبيع أسماء المحافظات (15 محافظة عراقية)
- تطبيع أنواع العقارات

### 5. Context Engine (محرك السياق)
**الملف:** `ai_context_engine.py`

- إدارة حالة المحادثة
- تتبع النية والمعلومات عبر الأدوار
- نظام الأسئلة الذكية
- إدارة تدفق المحادثة
- انتهاء صلاحية السياق (ساعتين)
- تفضيلات المستخدم
- تاريخ البحث

### 6. Semantic Search (البحث الدلالي)
**الملف:** `ai_semantic_search.py`

- محرك بحث هجين (Hybrid Search)
- دمج البحث المنظم مع البحث الدلالي
- نظام ترتيب النتائج الذكي
- تفسير سبب اختيار النتيجة
- دعم البحث عن العقارات، الفنادق، الخدمات
- تقييمات المطابقة
- نظام التعلم من المحادثات

### 7. Conversation Manager (مدير المحادثات)
**الملف:** `ai_conversation_manager.py`

- المنسق الرئيسي لجميع المكونات
- معالجة الرسائل بشكل متكامل
- دعم تعديل الطلبات
- استجابات باللهجة العراقية الطبيعية
- نظام الملاحظات من المستخدمين
- إحصائيات شاملة

## الميزات الرئيسية

### فهم اللغة واللهجة
- ✅ فهم الفصحى العربية
- ✅ فهم اللهجة العراقية
- ✅ تصحيح الأخطاء الإملائية
- ✅ فهم الكلام المختصر
- ✅ فهم الكلام الطبيعي غير المرتب

### معالجة السياق
- ✅ تذكر المعلومات السابقة
- ✅ فهم المراجع (مثل "خليه")
- ✅ تتبع المحادثة متعددة الخطوات
- ✅ دعم تعديل الطلبات
- ✅ منع تكرار الأسئلة

### نظام الثقة
- ✅ نظام ثقة لكل تحليل
- ✅ معالجة الثقة المنخفضة
- ✅ طلب التوضيح عند الحاجة
- ✅ قابل للتعديل ديناميكيًا

### البحث الذكي
- ✅ بحث هجين (منظم + دلالي)
- ✅ ترتيب النتائج الذكي
- ✅ تفسير اختيار النتائج
- ✅ دعم التفضيلات
- ✅ الاعتماد على قاعدة البيانات الحقيقية

### التعلم المستمر
- ✅ تسجيل التصحيحات
- ✅ نظام الملاحظات
- ✅ تصدير بيانات التدريب
- ✅ إحصائيات الأداء
- ✅ خط أنابيب التدريب المستقبلي

## أمثلة الاستخدام

### 1. شراء عقار
```
المستخدم: أريد بيت بالبصرة
المساعد: وشكد ميزانيتك تقريباً؟
المستخدم: 150 مليون
المساعد: تفضل بيت لو شقة؟
المستخدم: بيت
المساعد: حلو، لكيت لك عدة عقارات...
```

### 2. تعديل الطلب
```
المستخدم: لا خلّي الميزانية 200 مليون
المساعد: تمام، رحت أبحث بكرة... [نتائج محدثة]
```

### 3. طلب مركب
```
المستخدم: أريد بيت بالعراق ويفضل البصرة، عندي تقريباً 180 مليون وأريد منطقة هادئة وقريبة من المدارس
المساعد: حلو، لقيت نتائج تناسب طلبك... [نتائج مفصلة]
```

### 4. سؤال عام
```
المستخدم: شنو تگدر تسوي؟
المساعد: أقدر أساعدك بشراء وبيع العقارات، البحث عن دلالين، الوظائف، الفنادق، السفر، الخدمات، البناء، المزادات والمنتجات.
```

## الـ API Endpoints

### المحادثة الرئيسية
```http
POST /api/chatbot/
Content-Type: application/json

{
  "message": "أريد بيت بالبصرة",
  "conversation_id": "unique_id"
}
```

### الملاحظات
```http
POST /api/chatbot/feedback/
Content-Type: application/json

{
  "conversation_id": "unique_id",
  "feedback": "positive",
  "message": "آخر رسالة"
}
```

### التصحيحات
```http
POST /api/chatbot/correction/
Content-Type: application/json

{
  "conversation_id": "unique_id",
  "original_intent": "buy_property",
  "corrected_intent": "sell_property",
  "message": "أريد أبيع"
}
```

### الإحصائيات
```http
GET /api/chatbot/statistics/
```

## الاختبار

تم إنشاء مجموعة اختبارات شاملة في `ai_assistant_tests.py`:

- ✅ 40 اختبار مختلف
- ✅ اختبارات اللهجة العراقية
- ✅ اختبارات نهاية إلى نهاية
- ✅ اختبارات الأداء
- ✅ اختبارات التعلم

## المعمارية القابلة للتوسع

النظام مصمم لدعم إضافة:

- ✅ نماذج LLM عربية
- ✅ نماذج Fine-Tuned
- ✅ نماذج Embedding
- ✅ قواعد بيانات Vector
- ✅ RAG (Retrieval-Augmented Generation)
- ✅ مصنفات Intent متقدمة
- ✅ نماذج NER
- ✅ نماذج التوصية
- ✅ Learning-to-Rank

بدون تغيير واجهة Chatbot.

## القواعد الذهبية

### 1. عدم الاختراع
- النظام لا يخترع بيانات
- جميع النتائج من قاعدة البيانات الحقيقية
- عدم عرض عقارات غير موجودة

### 2. أمان البيانات
- فصل البيانات الشخصية عن بيانات التدريب
- عدم استخدام المحادثات مباشرة للتدريب
- تصفية البيانات الحساسة

### 3. تحسين مستمر
- التعلم من التصحيحات
- نظام الملاحظات
- تصدير بيانات التدريب
- إحصائيات الأداء

## المتطلبات التقنية

- Python 3.11+
- Django 4.x
- (اختياري) مكتبات ML المستقبلية:
  - transformers
  - sentence-transformers
  - faiss-cpu
  - scikit-learn

## التشغيل

### تفعيل الخادم
```bash
python manage.py runserver 127.0.0.1:8000
```

### تشغيل الاختبارات
```bash
python manage.py test properties.ai_assistant_tests
```

## المستقبل

### المرحلة القادمة
1. إضافة نموذج Arabic LLM حقيقي
2. تطبيق Embeddings للبحث الدلالي
3. إضافة قاعدة بيانات Vector
4. تطبيق RAG للمعلومات الخاصة بالموقع
5. نظام توصيات متقدم
6. لوحة تحكم AI

### التحسينات
1. فهم أفضل للعبارات المعقدة
2. دعم لغات إضافية
3. إجراءات أمان متقدمة
4. تحسين الأداء
5. دعم الصوت

## الخلاصة

تم تحويل المساعد من نظام Chatbot بسيط إلى نظام AI حقيقي قادر على:

- فهم اللغة العربية واللهجة العراقية
- معالجة سياق المحادثة
- تقديم استجابات طبيعية
- البحث الذكي في قاعدة البيانات
- التعلم من المستخدمين
- قابل للتوسع إلى نماذج Deep Learning

النظام جاهز للاستخدام ويمكن تطويره بشكل مستمر بناءً على احتياجات المستخدمين.